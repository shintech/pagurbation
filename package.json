{
  "name": "pagurbation",
  "version": "0.0.2",
  "description": "Server side pagination",
  "main": "build/pagurbation.js",
  "repository": "https://github.com/shintech/pagurbation.git",
  "author": "Michael Prather",
  "license": "MIT",
  "scripts": {
    "build": "babel -D -d build/ src/",
    "prepublish": "standard && npm run build"
  },  
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015-node6": "^0.4.0",
    "babel-preset-latest": "^6.24.0",
    "standard": "^10.0.1"
  },
  "engines": {
    "node": ">=6"
  }
}
